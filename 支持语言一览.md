# 代码知识图谱 — 支持语言一览

> 代码知识图谱（codegraph）支持 **34 种编程语言和标记语言**，
> 每种语言可提取函数/类/方法/变量/导入等实体，构建结构化的代码知识图谱。
> 分为三类解析器：**Go 专用**、**JS/TS 专用**、**通用正则解析器**。

---

## 一、专用解析器（AST 级精确解析，3 种）

| 语言 | 扩展名 | 解析引擎 | 提取实体 |
|------|--------|---------|----------|
| **Go** | `.go` | `go/parser` + `go/ast` | 包、文件、函数、方法、结构体、接口、字段、变量、常量、导入语句、函数调用 |
| **JavaScript** | `.js` `.jsx` `.mjs` | `goja/parser` | 函数、类、方法（class.method）、变量（const/let/var） |
| **TypeScript** | `.ts` `.tsx` | `goja/parser` | 同上 |

---

## 二、正则解析器（行级精确匹配，31 种）

### 2.1 系统编程语言

| 语言 | 扩展名 | 提取实体 |
|------|--------|----------|
| Rust | `.rs` | `fn`、`struct`、`enum`、`trait`、`impl` 方法（brace-stack 追踪）、`use` 导入、`const`/`static` |
| C | `.c` `.h` | 函数、`struct`、`#include`、`#define` 宏、`typedef`、`enum` |
| C++ | `.cpp` `.hpp` `.cc` `.cxx` `.hh` `.hxx` | 同 C |
| Zig | `.zig` | `fn`、`struct`、`const`、`var`、`test` |

### 2.2 JVM 语言

| 语言 | 扩展名 | 提取实体 |
|------|--------|----------|
| Java | `.java` | class、interface、method、function、import |
| Kotlin | `.kt` `.kts` | class、`fun`、import、val/var |
| Scala | `.scala` | class/object/trait、`def`、val、import、`type` |

### 2.3 .NET 与移动端

| 语言 | 扩展名 | 提取实体 |
|------|--------|----------|
| C# | `.cs` | class/struct/interface/record、method、function、using |
| Swift | `.swift` | class/struct/protocol/enum、`func`、import |
| Dart | `.dart` | class/mixin/extension、function、import、final/const/var |

### 2.4 脚本语言

| 语言 | 扩展名 | 提取实体 |
|------|--------|----------|
| Python | `.py` | `def` 函数、`class`、变量、`import`/`from` 导入 |
| Ruby | `.rb` | class/module、`def` 方法/函数、require |
| PHP | `.php` | class/interface/trait、function（含类方法归属） |
| Lua | `.lua` | `function`、require、local |
| R | `.r` `.R` | `function`、library、`<-` 赋值 |
| Bash/Shell | `.sh` `.bash` `.zsh` | `function`/`()` 函数、变量、source |

### 2.5 数据与标记语言

| 语言 | 扩展名 | 提取实体 |
|------|--------|----------|
| HTML | `.html` `.htm` | `<tag>` 标签、`.class`、`#id`、`<Component>` 组件 |
| CSS/SCSS/SASS/LESS | `.css` `.scss` `.sass` `.less` `.styl` | 选择器、`@keyframes`、`@mixin`、`@import` |
| JSON | `.json` | 顶层 key（作为变量） |
| YAML | `.yaml` `.yml` | 顶层 key（作为变量） |
| TOML | `.toml` | `[table]`（作为结构体）、key（作为变量） |
| Markdown | `.md` `.markdown` `.mdown` `.mdwn` | 标题（h1 函数，h2+ 方法）、代码围栏 |

### 2.6 配置文件与 IaC

| 语言 | 扩展名 | 提取实体 |
|------|--------|----------|
| Dockerfile | `Dockerfile` `.dockerfile` | FROM stage、ARG、ENV、LABEL |
| Makefile | `Makefile` `makefile` `.mk` | target（函数）、变量、include |
| HCL/Terraform | `.tf` `.hcl` | resource、data、variable、output、module、provider |

### 2.7 数据库与 API

| 语言 | 扩展名 | 提取实体 |
|------|--------|----------|
| SQL | `.sql` | CREATE TABLE、CREATE FUNCTION/PROCEDURE/TRIGGER/VIEW |
| GraphQL | `.graphql` `.gql` | type、interface、input、enum、scalar、directive |

### 2.8 前端框架

| 语言 | 扩展名 | 提取实体 |
|------|--------|----------|
| Vue | `.vue` | script 标签中提取 export function/component |
| PowerShell | `.ps1` `.psm1` `.psd1` | function、class、`$variable`、import-module |

### 2.9 构建系统

| 语言 | 扩展名 | 提取实体 |
|------|--------|----------|
| CMake | `CMakeLists.txt` `.cmake` | function、macro、set、option、project、`add_executable`/`add_library` |

---

## 三、架构总览

```
Builder (编排器)
  ├── .go               → GoBuilder.ParseFile()         (go/parser AST)
  ├── .js/.ts/.jsx/.mjs → JSBuilder.ParseFile()         (goja/parser AST)
  ├── .py               → PyBuilder.ParseFile()         (正则规则)
  ├── .rs/.java/.c/.cpp/.cs → LangBuilder.ParseFile()   (正则规则)
  ├── .rb/.php/.swift/.kt/.dart/.lua → LangBuilder      (正则规则)
  ├── .sh/.sql/.vue/.zig/.scala/.ex/.r → LangBuilder    (正则规则)
  ├── .html/.css/.json/.yaml/.md/.toml → LangBuilder    (正则规则)
  ├── Dockerfile/Makefile/CMakeLists.txt → LangBuilder   (正则规则)
  ├── .tf/.graphql/.ps1 → LangBuilder                   (正则规则)
  └── .r → LangBuilder                                  (正则规则)
```

---

## 四、实体类型

所有语言提取的统一实体：

| 实体类型 | 含义 | 对应关系 |
|---------|------|---------|
| `EntityFunction` | 函数/宏 | `RelContains`（文件包含） |
| `EntityMethod` | 方法 | `RelDefines`（类型定义） |
| `EntityStruct` | 类/结构体 | `RelContains`（文件包含） |
| `EntityInterface` | 接口/trait/protocol | `RelContains` |
| `EntityType` | 类型别名 | `RelContains` |
| `EntityVariable` | 变量/属性 | `RelContains` |
| `EntityConstant` | 常量 | `RelContains` |
| `EntityField` | 结构体字段 | `RelContains` |
| `EntityImport` | 导入语句 | `RelImports`（文件导入） |
| `EntityPackage` | 包/模块/命名空间 | `RelContains`（包包含文件） |
| `EntityCallSite` | 函数调用点 | `RelCalls`（调用者→调用点） |
| `EntityCommit` | Git 提交 | `RelModifiedBy` |

> 该表格生成于 2026-07-14
> 参阅 `pkg/codegraph/graph.go` 查看完整实体类型定义。
