# 代码知识图谱 — 支持语言总表

代码知识图谱支持 **34 种编程语言和标记语言**，覆盖主流开发场景。以下按语言名排序。

| 语言 | 扩展名 | 解析器类型 | 提取实体 |
|------|--------|-----------|----------|
| Bash/Shell | `.sh` `.bash` `.zsh` | 正则 | 函数、变量、source 导入 |
| C | `.c` `.h` | 正则 | 函数、结构体、include、宏、typedef、enum |
| C# | `.cs` | 正则 | class/struct/interface/method/function/using |
| C++ | `.cpp` `.hpp` `.cc` `.cxx` `.hh` `.hxx` | 正则 | 函数、类/结构体、include、typedef、宏 |
| CMake | `CMakeLists.txt` `.cmake` | 正则 | function/macro/set/option/project/target |
| CSS/SCSS/SASS/LESS | `.css` `.scss` `.sass` `.less` `.styl` | 正则 | 选择器、@keyframes、@mixin、@import |
| Dart | `.dart` | 正则 | class/mixin/function/import/变量 |
| Dockerfile | `Dockerfile` `.dockerfile` | 正则 | FROM stage/ARG/ENV/LABEL |
| Elixir | `.ex` `.exs` | 正则 | defmodule/def/defp/import |
| Go | `.go` | go/parser AST | 包/文件/函数/方法/结构体/接口/字段/变量/常量/导入/调用 |
| GraphQL | `.graphql` `.gql` | 正则 | type/interface/input/enum/scalar/directive |
| HCL/Terraform | `.tf` `.hcl` | 正则 | resource/data/variable/output/module/provider |
| HTML | `.html` `.htm` | 正则 | 标签、class、id、组件 |
| Java | `.java` | 正则 | class/interface/method/function/import |
| JavaScript | `.js` `.jsx` `.mjs` | goja/parser AST | 函数、类、方法、变量 |
| JSON | `.json` | 正则 | 顶层 key |
| Kotlin | `.kt` `.kts` | 正则 | class/fun/import/val/var |
| Lua | `.lua` | 正则 | function/require/local |
| Makefile | `Makefile` `makefile` `.mk` | 正则 | target、变量、include |
| Markdown | `.md` `.markdown` `.mdown` `.mdwn` | 正则 | 标题、代码围栏 |
| PHP | `.php` | 正则 | class/interface/trait/function |
| PowerShell | `.ps1` `.psm1` `.psd1` | 正则 | function/class/变量/import-module |
| Python | `.py` | 正则 | def 函数、class、变量、import/from |
| R | `.r` `.R` | 正则 | function/library/变量赋值 |
| Ruby | `.rb` | 正则 | class/module/def/require |
| Rust | `.rs` | 正则 | fn/struct/enum/trait/impl 方法/use/const |
| Scala | `.scala` | 正则 | class/object/trait/def/val/import/type |
| SQL | `.sql` | 正则 | CREATE TABLE/FUNCTION/PROCEDURE/VIEW |
| Swift | `.swift` | 正则 | class/struct/protocol/enum/func/import |
| TOML | `.toml` | 正则 | table、key |
| TypeScript | `.ts` `.tsx` | goja/parser AST | 函数、类、方法、变量 |
| Vue | `.vue` | 正则 | script 中 export function/component |
| YAML | `.yaml` `.yml` | 正则 | 顶层 key |
| Zig | `.zig` | 正则 | fn/struct/const/var/test |

> 总共 **34 种语言**。其中 Go/JS/TS 使用 AST 级精确解析，其余使用行级正则匹配。
> 所有实体统一为 12 种类型（函数/方法/结构体/接口/类型别名/变量/常量/字段/导入/包/调用点/提交）。
> 生成日期：2026-07-14
