fix: load the real companion frontend + flex icon estimates

User report: the probe/main loaded cmd/desktop/web-ui/dist (a stale
hand-built copy) instead of the cmd/companion frontend; the review
shield never drew, chat-input buttons were packed tight with no gap,
and the send button overflowed the input border.

1. companion/web-ui/vite.config.js now builds a single IIFE bundle with
   minify:false (no dynamic-import chunks, no ES module statements) so
   the wb-ui JS engine can execute the REAL companion frontend. All 18
   desktop_probe probes switched distDir to cmd/companion/web-ui/dist.

2. flex intrinsicContentWidth: replaced elements (svg with width="N"
   attribute, img) measured 0 from their (empty) children, so flex items
   with a 12px svg icon were sized ~3px under content — .obtn buttons
   drew 38px vs Edge's 53px and their content overflowed into neighbors.
   Now svg/img read their width attribute as intrinsic width.

3. Effect (verified against Edge headless rendering the same companion
   frontend):
   - review shield: green-ink 6 vs Edge 7 (was not drawn at all)
   - .obtn row: gap 2px between buttons, no overlap (was -1px overlap)
   - send-btn: x=965 (right edge 1009 == content box), was x=981 with
     right edge 1025 overflowing the input-wrapper border by 3px
   - ibb-btns estimate 184→260 closes to actual, space-between no
     longer shoves send-btn right by 16px
   - pixel diff send-btn region 1291→348, input row band 5285→3991

Full go test green; GEO 13/13.
